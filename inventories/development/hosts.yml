---
# development environment inventory
# auto-generated: Mon Jul 28 02:20:24 KST 2025

all:
  vars:
    ansible_user: ubuntu
    ansible_ssh_private_key_file: ../../keys/ssh/development/mitum_key
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
    
    # Mitum settings
    mitum_environment: development
    mitum_network_id: mitum-test
    mitum_chain_id: 100

mitum_nodes:
  hosts:
    node0:
